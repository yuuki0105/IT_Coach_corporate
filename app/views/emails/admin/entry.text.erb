＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
【エントリー内容】

名前
<%= @entry.name %>

メールアドレス
<%= @entry.email %>

電話番号
<%= @entry.telephone %>

サービスを知ったきっかけ （複数選択可）
<% @entry.source_routes.each do |source_route| %>
<%= source_route.title %>
<% end %>

保有スキル （複数選択可）
<% BigCategory.all.each do |big_category| %>
  [<%= big_category.name %>]
  <% @entry.categories.where(big_category_id: big_category.id).each do |category| %>
    <%= category.name %>
  <% end %>
<% end %>

得意なこと
<%= @entry.specialty %>

その他
<%= @entry.other %>

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝